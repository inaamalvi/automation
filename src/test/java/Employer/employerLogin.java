package Employer;

import java.io.IOException;

import org.testng.Assert;
import org.testng.annotations.Test;

import coreClasses.BaseTestScript;
import employer_PageObjects.Employer_LoginPage;


public class employerLogin extends BaseTestScript {
	/*
	Employer_Loginpage emp_login;

	@Test
	public void successful_login() throws InterruptedException, IOException {

		emp_login = new Employer_Loginpage(BaseTestScript.driver);
		emp_login.login();
		Thread.sleep(5000);
		if (emp_login.verifyUseronLoginPage())
		{
			System.out.println("User logged in successfully");
			
		}
		else
		{
			System.out.println("User login failed");
			Assert.assertTrue(false);
		}
			
		
		Thread.sleep(10000);
		
		
		// page title and compare
		
		//Assert.assertEquals(true, emp_reg.successRegistrationMethod().equalsIgnoreCase("Success in Registration"));
		//https://www.ittoptalent.com/employee/company_jobs

	}*/
}
